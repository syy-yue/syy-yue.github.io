package main

import "fmt"

func main() {
	a := make([]int,5,5)
	fmt.Println(a)
/*
//	k := a[:6]
	b := a[:5]
	c := a[:4]
	d := a[:3]
	e := a[:2]
	f := a[:1]
	g := a[:0]
	
	fmt.Println(cap(a), cap(b),cap(c),cap(d),cap(e),cap(f),cap(g))

	h := a[:]
	i := a[1:]
	j := a[2:]
	k := a[3:]
	l := a[4:]
	m := a[5:]
	fmt.Println(l)
	
	fmt.Println(cap(a), cap(h),cap(i),cap(j),cap(k),cap(l),cap(m))
*/
	
}
